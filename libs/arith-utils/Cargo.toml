[package]
name = "arith-utils"
version = "0.2.0"
authors = ["Dock.io"]
edition = "2021"

[dependencies]
num-traits = { version = "0.2.14", default-features = false }

[features]
default = ["std"]
std = ["num-traits/std"]
